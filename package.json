{
  "name": "seeksoul-online-document",
  "private": true,
  "type": "module",
  "scripts": {
    "docs:prepare": "node scripts/prepare-vitepress-docs.mjs && node scripts/process-doc-images.mjs",
    "docs:dev": "vitepress dev docs --host 127.0.0.1",
    "docs:build": "vitepress build docs",
    "docs:preview": "vitepress preview docs --host 127.0.0.1"
  },
  "devDependencies": {
    "sharp": "^0.34.5",
    "vitepress": "^1.6.4"
  }
}
